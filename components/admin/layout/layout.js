import React, {Fragment, useEffect} from 'react';

const LayoutAdmin = (props) => {
    useEffect(() => {

    }, []);
    return (
        <Fragment>
            {props.children}
        </Fragment>
    );
};

export default LayoutAdmin;